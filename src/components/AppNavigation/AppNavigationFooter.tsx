import {FC, ReactElement} from 'react';
import {CardFooter} from '@/components/ui/card';

const AppNavigationFooter: FC = (): ReactElement => {
  return (
    <CardFooter className={'overflow-hidden'}>

    </CardFooter>
  );
};

export default AppNavigationFooter;
